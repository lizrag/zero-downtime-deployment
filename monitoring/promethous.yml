global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yaml"

scrape_configs:
  - job_name: "auth-service"
    static_configs:
      - targets: ["auth-service:80"]
    metrics_path: "/metrics"